<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= page_title %>
      ·
      <%= site_title %></title>
    <meta name="description" content="<%= description %>">
    <link rel="stylesheet" href="assets/style.css">
    <style>:root { --accent: <%= accent_color %>; }</style>
  </head>
  <body>
    <div class="page">
      <aside class="sidebar">
        <a href="/lct" style="text-decoration: none; color: inherit;">
          <div class="brand">
            <div class="logo"><%= site_title %></div>
            <div class="tagline"><%= tagline %></div>
          </div>
        </a>
        <nav class="nav">
          <%= nav_html %>
        </nav>
        <% if links.any? %>
          <div class="nav-section-title">Links</div>
          <ul class="nav-links">
            <% links.each do |link| %>
              <li><a href="<%= link['url'] %>"><%= link["label"] %></a></li>
            <% end %>
          </ul>
        <% end %>
      </aside>
      <main class="content">
        <header class="topbar">
          <div class="breadcrumbs"><%= breadcrumb_html %></div>
          <div class="topbar-actions">
            <% if repo_url %>
              <a class="button ghost" href="<%= repo_url %>">GitHub</a>
            <% end %>
            <button class="button theme-toggle" type="button" data-theme-toggle>Toggle theme</button>
          </div>
        </header>
        <article class="doc">
          <%= content %>
        </article>
        <footer class="footer">
          <span>Generated by Bashly ·
            <%= Time.now.strftime("%Y") %></span>
          <% if repo_url %>
            <span>·</span>
            <a href="<%= repo_url %>">View source</a>
          <% end %>
        </footer>
      </main>
    </div>
    <script src="assets/app.js"></script>
  </body>
</html>
