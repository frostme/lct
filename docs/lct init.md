# lct init

Initialize lct configuration

## Usage

```bash
lct init
```

## Examples

```bash
lct init
```

If you provide a remote repository during initialization, lct will clone it into `~/.local/share/lct/remote` and seed your local config and package bundle files (for example `config.yaml` and `Brewfile`) so you can bootstrap a new machine immediately.

