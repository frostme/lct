# # These are for shell setup scripts
# autoload -Uz +X bashcompinit && bashcompinit
#
# eval "$(lct completions)"
# eval "$(zoxide init zsh)"
# eval "$(zellij setup --generate-auto-start zsh)"
eval "$(lct setup)"
